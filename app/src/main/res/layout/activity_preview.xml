<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/solarized_base03"
    tools:context=".PreviewActivity">

    <!-- Web 3D Previewer WebView Container -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/solarized_base03">

        <WebView
            android:id="@+id/webview_renderer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/solarized_base03" />

        <!-- Floating Loading Overlay Container -->
        <LinearLayout
            android:id="@+id/ll_loading_overlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="16dp"
            android:background="@drawable/bg_loading_dialog"
            android:elevation="6dp"
            android:visibility="visible">

            <ProgressBar
                android:id="@+id/pb_loading"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:indeterminateTint="@color/solarized_cyan"
                android:visibility="visible" />

            <TextView
                android:id="@+id/tv_loading_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:textColor="@color/solarized_base1"
                android:textSize="14sp"
                android:textStyle="bold"
                android:text="@string/loading" />
        </LinearLayout>

    </FrameLayout>

    <!-- Top Floating Actions Overlay -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:orientation="horizontal">

        <!-- Camera Switch (透视 / 正交) -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_camera_mode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:contentDescription="@string/view_mode"
            app:backgroundTint="@color/solarized_base02"
            app:fabSize="mini"
            app:srcCompat="@android:drawable/ic_menu_view"
            app:tint="@color/solarized_cyan" />

        <!-- Reset Camera but maintain Angle and zoom -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_reset_camera"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:contentDescription="@string/view_reset"
            app:backgroundTint="@color/solarized_base02"
            app:fabSize="mini"
            app:srcCompat="@android:drawable/ic_menu_revert"
            app:tint="@color/solarized_yellow" />

        <!-- Region Selector Switch -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_region_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/view_region"
            app:backgroundTint="@color/solarized_base02"
            app:fabSize="mini"
            app:srcCompat="@android:drawable/ic_dialog_map"
            app:tint="@color/solarized_green" />

    </LinearLayout>

    <!-- Draggable Bottom Sheet Layer for block counts -->
    <include
        android:id="@+id/bottom_sheet_root"
        layout="@layout/bottom_sheet_statistics" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
